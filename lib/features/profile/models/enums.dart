enum ActivityVisibility { hidden, coarse }
enum PresenceOptIn { off, activeNow }

enum PreferenceIntensity { curious, enjoys, deeplyEnjoys, favorite }

enum NominationStatus { offered, accepted, declined }
